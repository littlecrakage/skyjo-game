# Flask Configuration
FLASK_CONFIG=development
SECRET_KEY=your-secret-key-here

# Database
# For development (SQLite)
# DATABASE_URL=sqlite:///skyjo.db

# For production (PostgreSQL)
# DATABASE_URL=postgresql://user:password@localhost/skyjo

# Port (for local testing, Render will override this)
PORT=5000
